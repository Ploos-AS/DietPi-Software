# Profiles

Profiles compose software items into reusable DietPi system roles.

The first planned family is honeypots, but profiles are not limited to
security use cases.

See `docs/PROFILE_SPEC.md`.
