# Software items

Each directory below this path represents one independently installable
software item.

Items use repository-local identifiers. They are not official DietPi software
IDs unless separately accepted upstream.

See `docs/PROFILE_SPEC.md`.
